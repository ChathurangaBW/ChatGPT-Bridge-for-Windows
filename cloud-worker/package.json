{
  "name": "@chatgpt-bridge/cloud-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/**/*.test.ts",
    "smoke": "node test/runtime-smoke.mjs && node test/security-smoke.mjs && node test/multiwindow-smoke.mjs && node test/authorize-ui-smoke.mjs",
    "smoke:production": "node test/runtime-smoke.mjs && node test/multiwindow-smoke.mjs",
    "build": "wrangler deploy --dry-run --outdir ../artifacts/cloud-worker",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^6.0.0",
    "ws": "8.21.1"
  }
}
