{
  "name": "chatgpt-bridge-for-windows",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "bridge",
    "vscode-extension"
  ],
  "scripts": {
    "dev:bridge": "npm run dev --workspace @chatgpt-bridge/bridge",
    "build": "npm run build --workspaces",
    "build:vscode": "npm run build --workspace @chatgpt-bridge/vscode-extension",
    "package:vscode": "node -e \"require('node:fs').mkdirSync('artifacts',{recursive:true})\" && npm run build:vscode && npm run package:vsix --workspace @chatgpt-bridge/vscode-extension",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspaces --if-present",
    "qa": "npm run typecheck && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
